Distribution of data (e.g., movie ratings) doesn't reflect what you'd observe in the real world

Q3:
  While Argo authors used map-reduce, that's no longer possible with switch to V8 -- must use combo of two queries
